<script>

let escolha = prompt("Esolha o tipo de conversão:\n1. Celsius para Fahrenheit\n2. Fahrenheit para Celsius");

let temperatura = number(prompt("digite sua temperatura"));

function convertr(temperatura, escolha){

if (escolja === n1){

return (9/5) * temperatura + 32; 
} else if (escolha "n2"){

return (5/9) * (temperatura - 32);
} else {

return "Escala de temperatura inválida.";
}

}

if ( escolha === "n1"){

alert("A temperatura convertida é : " + converter (temperatura, "n1") + "  )









</script>